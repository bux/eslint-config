{
  "name": "@bux/eslint-config-repo",
  "version": "0.0.6",
  "private": true,
  "packageManager": "pnpm@7.2.1",
  "author": "Jo David",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@bux/eslint-config": "*",
    "bumpp": "^9.2.0",
    "eslint": "^8.49.0",
    "typescript": "^5.2.2"
  }
}
