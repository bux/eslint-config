{
  "name": "@bux/eslint-config",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@7.2.1",
  "description": "Jo's custom ESLint config (Based on antfu's config)",
  "author": "Jo David",
  "license": "MIT",
  "homepage": "https://github.com/bux/eslint-config",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "peerDependencies": {
    "eslint": ">=7.4.0"
  },
  "scripts": {
    "lint": "eslint .",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.25.1",
    "bumpp": "^8.2.1",
    "eslint": "^8.19.0",
    "typescript": "^4.7.4"
  }
}
